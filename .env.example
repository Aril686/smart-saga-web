# ==========================================================
# 🚀 VARIABEL UNTUK RAILWAY (DASHBOARD LAYANAN WEB)
# ==========================================================

# --- 1. SETTING UTAMA ---
PORT=3000
SESSION_SECRET=smart-saga-rahasia-banget-99

# --- 2. DATABASE (OPSI A: SEKALI TEMPEL - REKOMENDASI) ---
# Jika Anda pakai MySQL Railway, cukup masukkan baris ini saja:
MYSQL_URL=${{MySQL.MYSQL_URL}}

# --- 2. DATABASE (OPSI B: MANUAL - JIKA OPSI A GAGAL) ---
# Jika ingin mengisi satu-satu, gunakan referensi otomatis ini:
# MYSQLHOST=${{MySQL.MYSQLHOST}}
# MYSQLUSER=${{MySQL.MYSQLUSER}}
# MYSQLPASSWORD=${{MySQL.MYSQLPASSWORD}}
# MYSQLDATABASE=${{MySQL.MYSQLDATABASE}}
# MYSQLPORT=${{MySQL.MYSQLPORT}}

# --- 3. MQTT (HIVEMQ CLOUD) ---
MQTT_SERVER=d4074ff835754387b943f21e95168512.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USER=school-absensi
MQTT_PASSWORD=School12
MQTT_TOPIC=absensi/rfid

# ==========================================================
# INSTRUKSI:
# 1. Buka tab 'Variables' di layanan WEB (bukan layanan MySQL).
# 2. Klik 'Raw Editor'.
# 3. Paste semua baris di atas.
# 4. Railway akan otomatis menyambungkan ke Database Anda!
# ==========================================================
